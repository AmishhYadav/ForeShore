/**
 * Placeholder — replaced by the Phase 6 shore-console build. Kept minimal so
 * `npm run build` and `npm run dev` succeed while that work is in progress.
 */
export default function ConsoleApp() {
  return (
    <div style={{ padding: 24 }}>
      <h1>FORESHORE — Console</h1>
      <p>Phase 6 shore console under construction.</p>
    </div>
  );
}
