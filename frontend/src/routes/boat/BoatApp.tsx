/**
 * Placeholder — replaced by the Phase 5 boat-UI build. Kept minimal so `npm run build`
 * and `npm run dev` succeed while that work is in progress.
 */
export default function BoatApp() {
  return (
    <div style={{ padding: 24 }}>
      <h1>FORESHORE — Boat</h1>
      <p>Phase 5 boat UI under construction.</p>
    </div>
  );
}
